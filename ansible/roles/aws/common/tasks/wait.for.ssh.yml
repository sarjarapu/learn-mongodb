---
- name: Wait for SSH to come up
  wait_for:
    host: "{{ ssh_instance_item.public_ip }}"
    port: 22
    delay: 60
    timeout: 320
    state: started
  # local_action: wait_for 
  #               host={{ ssh_instance_item.public_ip }} 
  #               port=22 
  #               state=started